{% extends 'base.jinja' %}

{% block content %}
    <div class="edit-sport-event">
        <h1 class="edit">Edit Sport Event</h1>
        <form action="/sportevents/{{ sport_event.id }}" method="post">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="{{ sport_event.name }}" required>
            
            <label for="sport">Sport:</label>
            <input type="text" id="sport" name="sport" value="{{ sport_event.sport }}" required>
            
            <label for="location">Location:</label>
            <input type="text" id="location" name="location" value="{{ sport_event.location }}" required>
            
            <label for="date">Date:</label>
            <input type="datetime-local" id="date" name="date" value="{{ sport_event.date }}" required>

            <button type="submit" class="update-btn">Update Event</button>
        </form>
    </div>
{% endblock %}
